import { LoginForm } from "@/components/auth/login-form";

const LoginPage = () => {
  return (
    <main className="relative h-full bg-zinc-100">
      <LoginForm />
    </main>
  );
};

export default LoginPage;
